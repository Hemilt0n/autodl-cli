# autodl-cli

AutoDL Pro command-line tool.

Current status: design and project skeleton.

## Development

This project is managed with `uv`.

```bash
uv run autodl --help
```

The implementation plan is in `docs/autodl-pro-cli-design.md`.
